# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit real secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# editor
.idea/
.vscode/*
!.vscode/extensions.json

# uploads written during local dev
/tmp-uploads/

# admin-panel: node_modules are per-workspace, and the SPA build output
# lands in public/admin — both are build artefacts, not source.
admin-panel/node_modules/
public/admin/

# Leftover from an earlier abandoned dashboard-migration attempt. Superseded
# by admin-panel/. Kept on disk (not deleted) but ignored so it doesn't clutter
# git status; delete manually once verified nothing else references it.
/dashboard/
